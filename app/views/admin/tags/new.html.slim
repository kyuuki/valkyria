- content_for :title, "タグ作成"

- content_for :breadcrumb_setting do
  - @breadcrumb = [ \
      { name: "Home", path: admin_root_path }, \
      { name: "タグ作成" } \
    ]

.row
  .col-lg-12
    = render "form", tag: @tag
          
